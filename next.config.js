module.exports = {
    images: {
      domains: ['assets.acme.com'],
    },
  }

  /**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images:{
        domains:['cdn.nba.com'],
    },
  }
  
  module.exports = nextConfig